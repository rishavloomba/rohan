CREATE TABLE london_metal (id INTEGER PRIMARY KEY AUTOINCREMENT, dt DATE DEFAULT (DATE()), au_am TEXT NOT NULL, au_pm TEXT NOT NULL, ag_pm TEXT NOT NULL, pt_am TEXT NOT NULL, pt_pm TEXT NOT NULL, pd_am TEXT NOT NULL, pd_pm TEXT NOT NULL);
