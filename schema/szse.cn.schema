CREATE TABLE szsc (id INTEGER PRIMARY KEY AUTOINCREMENT, dt DATE DEFAULT (DATE()), name TEXT NOT NULL, today TEXT NOT NULL, delta TEXT NOT NULL, percent TEXT NOT NULL, hvalue TEXT NOT NULL, hdate TEXT NOT NULL);
CREATE TABLE szzb (id INTEGER PRIMARY KEY AUTOINCREMENT, dt DATE DEFAULT (DATE()), name TEXT NOT NULL, today TEXT NOT NULL, delta TEXT NOT NULL, hvalue TEXT NOT NULL, hdate TEXT NOT NULL);
CREATE TABLE zxb (id INTEGER PRIMARY KEY AUTOINCREMENT, dt DATE DEFAULT (DATE()), name TEXT NOT NULL, today TEXT NOT NULL, delta TEXT NOT NULL, hvalue TEXT NOT NULL, hdate TEXT NOT NULL);
CREATE TABLE cyb (id INTEGER PRIMARY KEY AUTOINCREMENT, dt DATE DEFAULT (DATE()), name TEXT NOT NULL, today TEXT NOT NULL, delta TEXT NOT NULL, hvalue TEXT NOT NULL, hdate TEXT NOT NULL);
