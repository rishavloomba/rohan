CREATE TABLE hsi_pe (dt DATE PRIMARY KEY, hsi TEXT NOT NULL, finance TEXT NOT NULL, utilities TEXT NOT NULL, properties TEXT NOT NULL, com_ind TEXT NOT NULL);
CREATE TABLE hscei_pe (dt DATE PRIMARY KEY, hscei TEXT NOT NULL);
CREATE TABLE hsi_dy (dt DATE PRIMARY KEY, hsi TEXT NOT NULL, finance TEXT NOT NULL, utilities TEXT NOT NULL, properties TEXT NOT NULL, com_ind TEXT NOT NULL);
CREATE TABLE hscei_dy (dt DATE PRIMARY KEY, hscei TEXT NOT NULL);
