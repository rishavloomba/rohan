CREATE TABLE future (id INTEGER PRIMARY KEY AUTOINCREMENT, dt DATE DEFAULT (DATE()), name TEXT NOT NULL, open TEXT NOT NULL, high TEXT NOT NULL, low TEXT NOT NULL, cloes TEXT NOT NULL, balance TEXT NOT NULL, volume TEXT NOT NULL, amount TEXT NOT NULL, position TEXT NOT NULL);
